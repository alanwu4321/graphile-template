web: yarn server start & yarn worker start
# TODO: seperate worker to its own process if needed
# worker: yarn worker start
# release: yarn db wipe-if-demo && yarn db migrate
# TODO: remove wipe-if-demo from the above line, so it should be simply:
release: yarn db migrate
